name: solidus_backend
description: A minimal Shelf + Drift backend for Solidus projects (sessions, tenancy, roles, TOTP 2FA).
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  collection: ^1.18.0
  crypto: ^3.0.3
  cryptography: ^2.7.0
  drift: ^2.22.1
  logging: ^1.2.0
  mailer: ^6.4.1
  path: ^1.9.0
  shelf: ^1.4.1
  shelf_cors_headers: ^0.1.5
  shelf_router: ^1.1.4
  sqlite3: ^2.4.6
  uuid: ^4.5.1

dev_dependencies:
  build_runner: ^2.4.13
  drift_dev: ^2.22.1
  lints: ^4.0.0
  test: ^1.25.8
