name: CI

on:
  pull_request:
  push:
    branches: ["master", "main"]

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Provision Dart SDK
        run: npm run provision:dart

      - name: Build
        env:
          DART: ${{ github.workspace }}/.dart-sdk/dart-sdk/bin/dart
        run: npm run build

      - name: Install Playwright browsers (Linux)
        if: runner.os == 'Linux'
        run: npx playwright install --with-deps chromium

      - name: UI smoke test (Playwright)
        if: runner.os == 'Linux'
        env:
          DART: ${{ github.workspace }}/.dart-sdk/dart-sdk/bin/dart
        run: npm run debug:ui:ci

      - name: Solid DOM semantics smoke (Playwright)
        if: runner.os == 'Linux'
        env:
          DART: ${{ github.workspace }}/.dart-sdk/dart-sdk/bin/dart
        run: npm run debug:solid-dom:ci

      - name: Solid DOM For semantics smoke (Playwright)
        if: runner.os == 'Linux'
        env:
          DART: ${{ github.workspace }}/.dart-sdk/dart-sdk/bin/dart
        run: npm run debug:solid-for:ci

      - name: Solid DOM Overlay semantics smoke (Playwright)
        if: runner.os == 'Linux'
        env:
          DART: ${{ github.workspace }}/.dart-sdk/dart-sdk/bin/dart
        run: npm run debug:solid-overlay:ci

      - name: Solid Dialog semantics smoke (Playwright)
        if: runner.os == 'Linux'
        env:
          DART: ${{ github.workspace }}/.dart-sdk/dart-sdk/bin/dart
        run: npm run debug:solid-dialog:ci

      - name: Solid Roving semantics smoke (Playwright)
        if: runner.os == 'Linux'
        env:
          DART: ${{ github.workspace }}/.dart-sdk/dart-sdk/bin/dart
        run: npm run debug:solid-roving:ci

      - name: Solid Popover semantics smoke (Playwright)
        if: runner.os == 'Linux'
        env:
          DART: ${{ github.workspace }}/.dart-sdk/dart-sdk/bin/dart
        run: npm run debug:solid-popover:ci

      - name: Solid Popover positioning smoke (Playwright)
        if: runner.os == 'Linux'
        env:
          DART: ${{ github.workspace }}/.dart-sdk/dart-sdk/bin/dart
        run: npm run debug:solid-popover-position:ci

      - name: Solid Popover flip smoke (Playwright)
        if: runner.os == 'Linux'
        env:
          DART: ${{ github.workspace }}/.dart-sdk/dart-sdk/bin/dart
        run: npm run debug:solid-popover-flip:ci

      - name: Solid Toast semantics smoke (Playwright)
        if: runner.os == 'Linux'
        env:
          DART: ${{ github.workspace }}/.dart-sdk/dart-sdk/bin/dart
        run: npm run debug:solid-toast:ci

      - name: Docs Forms suite (Playwright)
        if: runner.os == 'Linux'
        env:
          DART: ${{ github.workspace }}/.dart-sdk/dart-sdk/bin/dart
        run: npm run debug:docs-forms-suite:ci

      - name: Docs Overlay suite (Playwright)
        if: runner.os == 'Linux'
        env:
          DART: ${{ github.workspace }}/.dart-sdk/dart-sdk/bin/dart
        run: npm run debug:docs-overlay-suite:ci

      - name: Docs UI suite (Playwright)
        if: runner.os == 'Linux'
        env:
          DART: ${{ github.workspace }}/.dart-sdk/dart-sdk/bin/dart
        run: npm run debug:docs-ui-suite:ci

      - name: Docs props hydration (Playwright)
        if: runner.os == 'Linux'
        env:
          DART: ${{ github.workspace }}/.dart-sdk/dart-sdk/bin/dart
        run: npm run debug:docs-props:ci

      - name: Upload UI debug artifacts (on failure)
        if: failure() && runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: ui-debug-artifacts
          if-no-files-found: ignore
          path: |
            .cache/debug-ui.png
            .cache/debug-ui-report.json
